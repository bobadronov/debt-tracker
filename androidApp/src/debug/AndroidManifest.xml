<?xml version="1.0" encoding="utf-8"?>
<!-- Debug-only: a throwaway host screen that renders the home-screen widget's RemoteViews so it
     can be inspected / screenshotted without placing it on a launcher. Not in the release build. -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application>
        <activity
            android:name=".widget.WidgetPreviewActivity"
            android:exported="true"
            android:label="Widget preview"
            android:theme="@android:style/Theme.Material.Light.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
